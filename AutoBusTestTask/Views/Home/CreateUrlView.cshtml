@model AutoBusTestTask.Models.Url

<h4>Url</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="CreateUrlView">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="OriginalUrl" class="control-label">Длинный URL</label>
                <input asp-for="OriginalUrl" class="form-control" />
                <span asp-validation-for="OriginalUrl" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Создать сокращенный URL" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
